#pragma once
#include <intrin.h>
#include "BitBoard.h"
/*
class M128I {
public:
	__m128i m128i;
	M128I() {};
	M128I(__m128i in);
	M128I(uint64 in);
	M128I(uint64 x, uint64 y, uint64 z, uint64 w);

	M128I operator+();
	M128I operator-();
	M128I operator~();

	M128I operator+(const int in);
	M128I operator+(const M128I &in);

	M128I operator-(const int in);
	M128I operator-(const M128I &in);

	M128I operator >> (const int shift);
	M128I operator << (const int shift);

	M128I operator|(const M128I &in);
	M128I operator&(const M128I &in);

};
*/